"""Project facts: the questions every tool over a codebase has to ask first.

Which projects exist, which files belong to one, whether anything changed, and
what the source says. Three tools answered the first two questions three
different ways -- this index's registry, an architecture checker's directory
listing, and a hygiene checker reading a scheduler's database -- and a fix to
one never reached the others. The answers live here now; the questions on top
of them stay with each tool.
"""
